<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<ControlTheme x:Key="SelectorListBoxItem"
					TargetType="ListBoxItem">
		<Setter Property="Background" Value="{StaticResource ParameterBackgroundCommonColor}"/>
		<Setter Property="Template">			
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type ListBoxItem}">
					<Border BorderBrush="{DynamicResource CommonBorderBrush}"
							x:CompileBindings="False"
							Height="75"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0,0,0,1" Margin="0">
						<ContentPresenter Content="{TemplateBinding Content}"
										  Name="content"
										  FontSize="34"
										  VerticalAlignment="Center"
										  Margin="42,0,0,0"/>
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
		<Style Selector="^:selected">
			<Setter Property="Background" Value="{StaticResource ParameterSelected}"/>			
		</Style>
	</ControlTheme>
</ResourceDictionary>
